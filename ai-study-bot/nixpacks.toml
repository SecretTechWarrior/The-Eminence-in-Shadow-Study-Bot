[phases.setup]
nixPkgs = [
  "python311",
  "python311Packages.pip",
  "ffmpeg"
]

[phases.install]
cmds = [
  "pip install --break-system-packages --no-cache-dir -r bot/requirements.txt"
]

[start]
cmd = "python bot/src/main.py"
