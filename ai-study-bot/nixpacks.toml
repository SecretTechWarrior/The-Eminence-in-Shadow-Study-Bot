[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "python -m venv /opt/venv",
  "/opt/venv/bin/pip install --upgrade pip",
  "/opt/venv/bin/pip install -r bot/requirements.txt"
]

[start]
cmd = "/opt/venv/bin/python bot/src/main.py"
